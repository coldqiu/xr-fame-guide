<!--index.wxml-->
<navigation-bar
  title="Weixin"
  color="black"
  background="#FFF"
></navigation-bar>
  <!-- back="{{false}}" -->
<xr-start
  disable-scroll
  id="main-frame"
  width="{{renderWidth}}"
  height="{{renderHeight}}"
  style="width:{{width}}px;height:{{height}}px;"
></xr-start>

